# SQLite works immediately. Replace with PostgreSQL in deployment:
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/freshfusion
DATABASE_URL=sqlite:///./freshfusion.db
CORS_ORIGINS=*
UPLOAD_DIR=../uploads
